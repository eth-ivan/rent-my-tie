<div class="container pt-5">
  <div>
    <div class="card-show-trip">
      <%= cl_image_tag(@tie.picture.key) %>
      <div class="card-show-trip-infos">
        <div>
          <h2><%= @tie.name %></h2>
          <div><%= @tie.description %></div>
        </div>
        <div class="card-show-pricing"><%= @tie.price_per_day %>$/day</div>
      </div>
    </div>
  </div>
  <div>
    <%= link_to 'Back', :back %>
  </div>
</div>
